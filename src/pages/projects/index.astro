---
import { getCollection } from 'astro:content'

import Layout from '../../layout/Layout.astro'
import Project from '../../components/main/Projects.astro'

const seo = await getCollection('seo')
---

<Layout title={seo[0].data.metaDescription}>
  <Project />
</Layout>

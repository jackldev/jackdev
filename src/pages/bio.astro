---
import { getCollection } from 'astro:content'

import Layout from 'layout/Layout.astro'
import Bios from 'components/main/Bio/Bio.astro'

const bio = await getCollection('bio')
---

<Layout title={bio[0].data.title}>
  <Bios />
</Layout>
